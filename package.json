{
  "name": "smart-parking-campus",
  "version": "1.0.0",
  "private": true,
  "description": "MVP de estacionamento inteligente para campus com MQTT + HTTP + Postgres",
  "workspaces": [
    "shared",
    "api",
    "ingestor",
    "simulator"
  ],
  "scripts": {
    "up": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "psql": "docker compose exec postgres psql -U parking -d parking",
    "mqtt:sub": "docker compose exec mosquitto mosquitto_sub -v -t 'campus/parking/#'"
  }
}
